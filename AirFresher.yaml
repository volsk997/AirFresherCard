type: custom:bubble-card
card_type: pop-up
hash: '#air_fresher'
margin_top_mobile: auto
margin_top_desktop: auto
show_header: false
close_by_clicking_outside: false
button_type: name
styles: |
  #root {
    border: 1px solid rgba(var(--rgb-primary-text-color),0.1);
    
    }
sub_button:
  main: []
  bottom: []
cards:
  - type: custom:mushroom-chips-card
    chips:
      - type: template
        content: Освежитель
        double_tap_action:
          action: none
        tap_action:
          action: none
        hold_action:
          action: none
        icon: fas:spray-can-sparkles
      - type: spacer
      - type: template
        entity: sensor.air_fresh_vol
        content: |
          {{states(entity)|float(0)|round(1)|replace(".",",")}}%
        double_tap_action:
          action: none
        tap_action:
          action: more-info
        hold_action:
          action: none
        icon: mdi:chart-line-variant
        card_mod:
          style:
            .: |
              {% set hover = 'true' %}
              {% set stating = 'false' %}
              {% include 'chip_settings_style.jinja' %}
      - type: template
        entity: switch.air_fresher
        double_tap_action:
          action: none
        tap_action:
          action: more-info
        hold_action:
          action: none
        icon: mdi:information
        card_mod:
          style:
            .: |
              {% set hover = 'true' %}
              {% set stating = 'false' %}
              {% include 'chip_settings_style.jinja' %}
      - type: back
        card_mod:
          style:
            .: |
              {% set hover = 'true' %}
              {% set stating = 'false' %}
              {% include 'chip_settings_style.jinja' %}
    card_mod:
      style:
        mushroom-back-chip:last-child$: |
          ha-state-icon {
            --card-mod-icon: mdi:close;
          }
        .: |
          {% include 'pop_up_title.jinja' %}
    grid_options:
      columns: 12
      rows: auto
  - type: custom:mushroom-chips-card
    chips:
      - type: template
        entity: switch.air_fresher
        tap_action:
          action: toggle
        hold_action:
          action: none
        double_tap_action:
          action: none
        icon: material-symbols-light:air-freshener
        icon_color: |2-
            {% if is_state(entity, 'on')%} blue
            {% else %} 1
            {% endif %}
        card_mod:
          style:
            .: |
              {% set scale = '1' %}
              {% set hover = 'true' %}
              {% set stating = 'true' %}
              {% set state_on = 'on' %}
              {% set color = 'rgba(120,120,120,0.2)' %}
              {% set hover_color_off = 'rgba(120,120,120,0.3) !important' %}
              {% include 'chip_settings_style.jinja' %}
    alignment: center
    card_mod:
      style:
        .: |
          {% include 'big_button_popup.jinja' %}
  - type: custom:horizontal-stack-in-card
    cards:
      - type: custom:mushroom-template-card
        features_position: bottom
        entity: input_number.period_raboty_osvezhitelia
        grid_options:
          columns: 12
          rows: auto
        color: |-
          {% if is_state(config.entity, '10.0')%} white
          {% else %} disabled
          {% endif %}
        tap_action:
          action: perform-action
          perform_action: input_number.set_value
          target:
            entity_id: input_number.period_raboty_osvezhitelia
          data:
            value: 10
        icon: tabler:number-10-small
        vertical: true
        icon_tap_action:
          action: none
        card_mod:
          style: |
            ha-card {
              zoom: 1.5;
              background: none !important;
            }
      - type: custom:mushroom-template-card
        features_position: bottom
        entity: input_number.period_raboty_osvezhitelia
        grid_options:
          columns: 12
          rows: auto
        color: |-
          {% if is_state(config.entity, '20.0')%} white
          {% else %} disabled
          {% endif %}
        tap_action:
          action: perform-action
          perform_action: input_number.set_value
          target:
            entity_id: input_number.period_raboty_osvezhitelia
          data:
            value: 20
        icon: tabler:number-20-small
        vertical: true
        icon_tap_action:
          action: none
        card_mod:
          style: |
            ha-card {
              zoom: 1.5;
              background: none !important;
            }
      - type: custom:mushroom-template-card
        features_position: bottom
        entity: input_number.period_raboty_osvezhitelia
        grid_options:
          columns: 12
          rows: auto
        color: |-
          {% if is_state(config.entity, '30.0')%} white
          {% else %} disabled
          {% endif %}
        tap_action:
          action: perform-action
          perform_action: input_number.set_value
          target:
            entity_id: input_number.period_raboty_osvezhitelia
          data:
            value: 30
        icon: tabler:number-30-small
        vertical: true
        icon_tap_action:
          action: none
        card_mod:
          style: |
            ha-card {
              zoom: 1.5;
              background: none !important;
            }
      - type: custom:mushroom-template-card
        features_position: bottom
        entity: input_number.period_raboty_osvezhitelia
        grid_options:
          columns: 12
          rows: auto
        color: white
        tap_action:
          confirmation:
            text: Обнулить освежитель?
          action: perform-action
          perform_action: counter.reset
          target:
            entity_id: counter.osvezhitel
        icon: mdi:restore-alert
        vertical: true
        icon_tap_action:
          action: none
        card_mod:
          style: |
            ha-card {
              zoom: 1.5;
              background: none !important;
            }
      - type: custom:mushroom-template-card
        features_position: bottom
        entity: input_number.emkost_ballona_osvezhitelia
        grid_options:
          columns: 12
          rows: auto
        color: white
        tap_action:
          action: more-info
        icon: mdi:cup
        vertical: true
        icon_tap_action:
          action: none
        card_mod:
          style: |
            ha-card {
              zoom: 1.5;
              background: none !important;
            }
    card_mod:
      style: |
        ha-card {
         background: none !important;
        }
popup_mode: default
card_layout: large
slide_to_close_distance: '500'
